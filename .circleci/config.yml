version: 2.1
jobs:
  construir:
    docker:
      - image: circleci/node:12.18.4
    steps:
      - checkout
      - run: cd backend
      - run: npm install
      - run: npm test
workflows:
  version: 2.1
    construir:
      jobs:
        - construir
