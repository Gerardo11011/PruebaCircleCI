version: 2.1
jobs:
  run_test:
    docker:
      - image: "circleci/node:10"
  steps:
    - checkout
    - run: cd backend
    - run: null
      name: install npm dependencies
      command: |
        npm install
    - run: null
      name: run test
      command: |
        npm test
workflows:
  build_test:
    jobs:
      - run_test
